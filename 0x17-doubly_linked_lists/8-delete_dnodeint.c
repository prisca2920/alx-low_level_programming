#include "lists.h"

/**
 * delete_dnodeint_at_index - deletes node at idx
 * @head: the list to be checked
 * @index: the int to be checked
 * Return: nothing
 */

int delete_dnodeint_at_index(dlistint_t **head, unsigned int index)
