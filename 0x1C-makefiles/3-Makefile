CC = gcc

SRC = main.c school.c

OBJ = ${SRC: .c = .o}

NAME = school

RM = rm -f

${NAME}: ${OBJ}
	${CC} ${OBJ} -o $@

all: ${NAME}

clean:
	rm -f ${OBJ} ${SRC}

oclean:
	rm -f ${OBJ}

fclean: 
	rm -f ${NAME} ${OBJ} ${SRC}

re: fclean all
