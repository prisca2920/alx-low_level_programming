CC = gcc

SRC = main.c school.c

OBJ = ${SRC: .c = .o}

NAME = school

RM = clean

${NAME}: ${OBJ}
	${CC} $^ -o $@

all: ${NAME}

clean:
	rm -f ${OBJ} ${SRC}

oclean:
	rm -f ${OBJ}

fclean: 
	rm -f ${NAME} ${OBJ} ${SRC}

re: ${NAME}
