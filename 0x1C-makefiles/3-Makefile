CC = gcc

SRC = main.c school.c

OBJ = ${SRC: .c = .o}

NAME = school

RM = rm -f

${NAME}: ${OBJ}
	${CC} ${OBJ} -o $@

all: ${NAME}

clean:
	${RM} ${OBJ} ${SRC}

oclean:
	${RM} ${OBJ}

fclean: 
	${RM} ${NAME} ${OBJ} ${SRC}

re: fclean all
