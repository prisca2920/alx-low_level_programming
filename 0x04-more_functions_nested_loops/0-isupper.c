#include "main.h"

/**
 * _isupper - checks for uppercase letters
 * @n: the character to be checked
 * Return: 1 if uppercase 0 if false
 */

int _isupper(int c)
{
	if ((c >= 65) && (c <= 65))
	{
		return (1);
	}

	else
	{
		return (0);
	}
}
